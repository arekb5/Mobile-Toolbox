<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.testappv2">

    <uses-permission android:name="android.permission.ACCESS_FINE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_COARSE_LOCATION" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.TestAppV2">
        <activity android:name=".Speedometer" android:parentActivityName=".MainActivity"></activity>
        <activity android:name=".Leveler" android:parentActivityName=".MainActivity"/>
        <activity android:name=".Compass" android:screenOrientation="portrait" android:parentActivityName=".MainActivity"/>
        <activity android:name=".LocationGPS" android:parentActivityName=".MainActivity"/>
        <activity android:name=".LocationLK" android:parentActivityName=".MainActivity"/>
        <activity android:name=".Light" android:parentActivityName=".MainActivity"/>
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <service
            android:name=".LocationLK"
            android:foregroundServiceType="location" />
    </application>

</manifest>